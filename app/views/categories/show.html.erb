<% all_posts = link_to('All Posts', posts_path) %>

<%= render 'share/content_title', title: "#{all_posts} &raquo; #{@category.name}".html_safe %>

<!-- Code similar to Home page index.html.erb. Copy paste from there since we have a post object. Same thing. -->

<% @category.posts.each do |post| %>
  <%= render 'posts/post', post: post %>
<%end%>

